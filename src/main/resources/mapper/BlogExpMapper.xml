<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.athome.mybatis.mapper.BlogExpMapper">
    <insert id="save" parameterType="com.athome.pojo.Blog">
        insert into blog(`name`,id_no) values(#{name},#{idNo})
    </insert>
</mapper>